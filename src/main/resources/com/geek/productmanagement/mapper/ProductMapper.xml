<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace = 'com.geek.productmanagement.mapper.ProductMapper'>
	<select id = 'findAllByStoreId' resultType = 'com.geek.productmanagement.dto.ProductListDto'>
		SELECT id, product_name, product_image ,
		child_category_name, 
		store_price, store_stock 
		FROM products
		
		INNER JOIN child_categories 
		ON products.child_category_id = child_categories.id
		
		INNER JOIN store_products
		ON storeproducts.store_id = store_products.id
		
		WHERE 
		
	</select>
  
</mapper>